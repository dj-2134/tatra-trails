// Copy this file to js/config.js and paste your Mapy.com API key.
// Get a free key at https://developer.mapy.com (create a project → API keys).
// The key is used client-side, so RESTRICT it by domain in the Mapy dashboard.
// In CI the key is injected from the MAPY_API_KEY GitHub Actions secret instead.
export const MAPY_API_KEY = "YOUR_MAPY_API_KEY";
